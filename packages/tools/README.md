# Structured Tools

Future home for controlled modeling and editing tools.

The preferred path is structured operations such as create, transform, assign material, place, inspect, validate, render, and snapshot. Arbitrary Python may exist later as an escape hatch, not the main execution path.

